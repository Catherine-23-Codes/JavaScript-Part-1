/*
Variable = name of storage location
eg: 
// Integers - they must be initialised with a variable
    age=23
    a=5

// Strings - they must be initialised with a variable and also within the double quotes
    name="catherine"
    animal="lion"
*/
